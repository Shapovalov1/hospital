<%= form_with(url: categories_path, method: :post) do |f| %>

  <div class="field">
    <%= f.label :category %><br />
    <%= f.text_field :name, autofocus: true, autocomplete: "name" %>
  </div>

  <div class="actions">
    <%= f.submit "Create" %>
  </div>
<% end %>